
  <div class="">
<div class="show_article  mx-auto">
  <div class="logo mb-3"><br>
     <div class=" ">
       <h5>
         <strong>Commenter:</strong><%= comment.commenter %>
       </h5>

       <h5>
         <strong>Comment:</strong><%= comment.body %>
         <% if @current_user && @current_user.id == @article.user_id  %>
         <%= link_to 'Delete', [comment.article, comment], method: :delete, data: { confirm: 'Are you sure?' } %>
       </h5>
       <% end %>
     </div>
  </div>
</div>
</div>
<!-- </div> -->
